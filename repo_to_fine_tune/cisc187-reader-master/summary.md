# Summary

Below is my summary of this repo so far:

This repository has subdirectories `data`, `models` and `train`. The `train` directory holds various codes such as a reader_tiny_original.py that was trained on CS 187 data. Additional files in the repository include one README.md file inside the models/ directory, providing instructions.

The context window has become slightly larger with ~0.5% (or ~640 bytes) utilized out of its maximum capacity.

As for our current progress:
*   **Step 1 implemented**: This indicates that an initial step required for building the CS 187 reader model is complete.
*   **Step 2 in progress...**: It seems we are currently working on implementing the next necessary step.

Note: I still need to review the CS 187 reader model and its performance metrics.

The context window has increased by ~0.9% (or ~640 bytes) with ~1k tokens used out of the maximum capacity of 128k tokens.